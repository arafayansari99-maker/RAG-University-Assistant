# Base URL the generated API client calls. Relative "/api" works in dev (via the
# Vite proxy below) and in production (via a host rewrite) - same code path.
VITE_API_BASE=/api

# Where the Vite dev server proxies /api. Must match the API's PORT.
VITE_API_PROXY=http://localhost:3000
